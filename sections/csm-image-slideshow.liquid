{{ 'flickity.min.css' | asset_url | stylesheet_tag }}

<style>
  .csm-image-flickity-slider {
    width: 100%;
    overflow: hidden;
  }

  .csm-image-flickity-slider .csm-image-carousel-cell {
    width: 100%;
    margin-right: 0;
  }

  .csm-image-flickity-slider .csm-image-carousel-cell img{
    width: 100%;
  }
</style>

<div class="csm-image-flickity-slider">
  {% for block in section.blocks %}
    <div class="csm-image-carousel-cell">
        {% if block.settings.image != blank %}
            <img
                class="desktop-img"
                src="{{ block.settings.image | image_url: width: 2000 }}"
            >
        {% endif %}

        {% if block.settings.mobile_image != blank %}
            <img
                class="mobile-img"
                src="{{ block.settings.mobile_image | image_url: width: 1000 }}"
            >
        {% else %}
            {% if block.settings.image != blank %}
            <img
                class="mobile-img"
                src="{{ block.settings.image | image_url: width: 1000 }}"
            >
            {% endif %}
        {% endif %}
    </div>
  {% endfor %}
</div>

{% assign show_arrows = false %}
{% assign show_dots = true %}

{% if section.settings.navigation_style == 'arrows' %}
  {% assign show_arrows = true %}
  {% assign show_dots = false %}
{% elsif section.settings.navigation_style == 'none' %}
  {% assign show_arrows = false %}
  {% assign show_dots = false %}
{% endif %}

<script
  src="{{ 'flickity.pkgd.min.js' | asset_url }}"
  defer
></script>
<script>
document.addEventListener('DOMContentLoaded', function () {
  new Flickity('.csm-image-flickity-slider', {
    wrapAround: true,
    autoPlay: 3000,
    pauseAutoPlayOnHover: true,
    prevNextButtons: {{ show_arrows }},
    pageDots: {{ show_dots }},
    draggable: true
  });
});
</script>

{% schema %}
{
  "name": "CSM Image Slideshow",
  "settings": [
    {
        "type": "select",
        "id": "navigation_style",
        "label": "Navigation Style",
        "default": "dots",
        "options": [
            { "value": "dots", "label": "Dots" },
            { "value": "arrows", "label": "Arrows" },
            { "value": "none", "label": "None" }
        ]
    },
    {
        "type": "range",
        "id": "mobile_height",
        "label": "Mobile Image Height",
        "min": 200,
        "max": 800,
        "step": 10,
        "default": 400,
        "unit": "px"
    }
  ],
  "blocks": [
    {
      "type": "slide",
      "name": "Slide",
      "settings": [
        {
            "type": "image_picker",
            "id": "image",
            "label": "Desktop Image"
        },
        {
            "type": "image_picker",
            "id": "mobile_image",
            "label": "Mobile Image"
        }
      ]
    }
  ],
  "max_blocks": 3,
  "presets": [
    {
      "name": "CSM Image Slideshow"
    }
  ]
}
{% endschema %}